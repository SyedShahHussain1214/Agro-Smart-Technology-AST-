plugins {
    id("com.android.application")
    id("kotlin-android")
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id("dev.flutter.flutter-gradle-plugin")
}

android {
    namespace = "com.example.agro_smart_tech"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.example.agro_smart_tech"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            // Keep all assets and code unminified for richer visuals
            signingConfig = signingConfigs.getByName("debug")
            // Disable resource shrinking and code minification
            // (R8/proguard disabled to bundle full resources per request)
            minifyEnabled = false
            shrinkResources = false
        }
        debug {
            // Debug defaults
        }
    }

    // Ensure no resource exclusions; package all defaults
    packagingOptions {
        // Keep standard settings; do not exclude common assets
    }
}

flutter {
    source = "../.."
}
apply plugin: 'com.google.gms.google-services'

